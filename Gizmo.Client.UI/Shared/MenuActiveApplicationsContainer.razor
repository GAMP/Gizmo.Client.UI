@namespace Gizmo.Client.UI.Shared
@inherits CustomDOMComponentBase
@using Gizmo.Web.Components
@using Gizmo.Client.UI.Components

<Drawer @bind-IsOpen="IsOpen">
    <div class="giz-active-apps">
        <div class="giz-active-apps__header">
            <div class="giz-heading">Active applications</div>
        </div>
        <div class="giz-active-apps__body giz-scrollbar--v">
            @foreach (var executable in ActiveApplicationsService.ViewState.Executables)
            {
                <MenuActiveApplicationCard Executable="@executable" />
            }
        </div>
    </div>    
</Drawer>
