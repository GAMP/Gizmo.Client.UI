@namespace Gizmo.Client.UI.Shared
@inherits ShellComponentBase

@* Renders nothing. Bridges the browser's focus verdict into ShellActivity and vetoes
   sleeping while a deployment is running. Main layout only - the login screen has no
   applications to run and its attract loop must keep playing. *@
