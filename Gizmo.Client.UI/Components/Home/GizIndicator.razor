@namespace Gizmo.Client.UI.Components
@using Gizmo.Web.Components

<!--carousel button-->
<div class="giz-carousel-indicator">

    @for (int i = 0; i < Size; i++)
    {
        var local_i = i;
        <button type="button" class="giz-carousel-indicator-btn @(local_i == SelectedIndex ? "active": "")" @onclick="@(() => OnClickButton(local_i))"></button>
    }
</div>
<!--/carousel button-->