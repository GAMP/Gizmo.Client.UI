@namespace Gizmo.Client.UI.Components
@inherits CustomDOMComponentBase

<GizDock>
    @foreach (var item in ViewState.Executables.Take(PopularItemsOptions.Value.MaxQuickLaunchExecutables))
    {
        <GizDockItem @key="@item" Executable="@item" />
    }
</GizDock>