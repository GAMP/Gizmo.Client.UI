@namespace Gizmo.Client.UI.Components
@inherits CustomDOMComponentBase

@if (!IsShopEnabled)
{
}
else if (_product.DisallowPurchase)
{
    <ClientTooltip Text="@_product.DisallowPurchaseReason" Class="giz-product-host-groups-tooltip">
        <ChildContent>
            <Button IsDisabled="true"
                    Color="ButtonColors.Accent"
                    Size="@Size"
                    IsFullWidth="@IsFullWidth"
                    Text="@ShellText.Get(ShellText.GEN_ADD_TO_CART)" />
        </ChildContent>
    </ClientTooltip>
}
@* A time package is bought outright rather than added to the cart.

   The cart is for goods: several at a time, paid for in one order. A time package is
   taken one at a time and is what the customer came for, so it has one step - Buy, then
   the purchase dialog. Same path as the button on the home board; the ordering lives in
   PackagePurchaseFlow. *@
else if (IsTimePackage)
{
    <Button Color="ButtonColors.Accent"
            Class="giz-product-card-primary-button"
            Size="@Size"
            IsFullWidth="@IsFullWidth"
            IsDisabled="@_buying"
            Text="@ShellText.Get(_buying ? ShellText.GEN_WORKING : ShellText.GEN_BUY)"
            @onclick="OnBuyPackageClickHandler" />
}
else
{
    if (_userCartProductViewState == null || _userCartProductViewState.Quantity == 0)
    {
        @* The short label ("В корзину"): the card is a sixth of the grid, and with the
           icon the stock phrase wrapped onto a second line. *@
        <Button Color="ButtonColors.Accent"
                Class="giz-product-card-primary-button"
                Size="@Size"
                IsFullWidth="@IsFullWidth"
                LeftSVGIcon="Icons.Plus"
                Text="@ShellText.Get(ShellText.GEN_ADD_TO_CART)"
                @onclick="OnAddProductButtonClickHandler" />
    }
    else
    {
        <QuantityPicker Maximum="@(_product.ProductType == ProductType.ProductTime ? 1 : int.MaxValue)"
                        Size="@Size"
                        Quantity="@_userCartProductViewState.Quantity"
                        OnAddQuantityButtonClick="OnAddQuantityButtonClickHandlerAsync"
                        OnRemoveQuantityButtonClick="OnRemoveQuantityButtonClickHandler"
                        OnClick="@((args) => OnClick.InvokeAsync(args))" />
    }
}