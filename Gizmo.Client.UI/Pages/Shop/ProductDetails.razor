@namespace Gizmo.Client.UI.Pages
@inherits CustomDOMComponentBase

@* One product, the whole story: picture, name, price and the buy control at the top;
   the small print (where it works, when it expires, when it can be bought) as plain
   facts under them; description, bundle contents and related goods below. The cart
   keeps its column on the right, as on the shop page. *@
@{
    var product = ViewState.Product;
    var isTime = product?.ProductType == ProductType.ProductTime;
    var isBundle = product?.ProductType == ProductType.ProductBundle;
    var time = isTime ? product.TimeProduct : null;
}

<div class="gg-pd-wrap">
    <div class="gg-pd @(CanPurchase ? "gg-pd--with-cart" : null)">
        <div class="gg-pd__page giz-scrollbar--v">

            <button type="button" class="gg-pd__back" @onclick="OnClickBackButtonHandler">
                <i class="ph-bold ph-arrow-left"></i>
                <span>@ShellText.Get(ShellText.GEN_BACK)</span>
            </button>

            @if (product is not null)
            {
                <section class="gg-pd__main">
                    <div class="gg-pd__art @(isTime ? "gg-pd__art--time" : null)">
                        <GizImage @key="product" ImageType="Gizmo.UI.ImageType.ProductDefault" ImageId="@product.DefaultImageId" ImageFitType="ImageFitType.Cover">
                            <EmptyResultPlaceholder>
                                @if (isTime)
                                {
                                    <div class="gg-pd__time">
                                        <b>@ProductHelpers.GetProductTimeNumber(product)</b>
                                        <span>@ProductHelpers.GetProductTimeText(product, LocalizationService)</span>
                                    </div>
                                }
                                else
                                {
                                    <div class="gg-pd__no-art"><i class="ph ph-image"></i></div>
                                }
                            </EmptyResultPlaceholder>
                            <ErrorPlaceholder>
                                <div class="gg-pd__no-art"><i class="ph ph-image-broken"></i></div>
                            </ErrorPlaceholder>
                        </GizImage>
                    </div>

                    <div class="gg-pd__info">
                        @if (!string.IsNullOrWhiteSpace(UserProductGroupViewState?.Name))
                        {
                            <div class="gg-cap">@UserProductGroupViewState.Name</div>
                        }
                        <h1 class="gg-pd__title">@product.Name</h1>

                        <div class="gg-pd__price"><ProductCardPrice @key="ProductId" Product="@product" /></div>

                        @if (CanPurchase)
                        {
                            <div class="gg-pd__buy">
                                <ProductQuantityPicker @key="ProductId" ProductId="@ProductId" Size="ButtonSizes.ExtraLarge" IsFullWidth="false" />
                                @if (product.DisallowPurchase && !string.IsNullOrWhiteSpace(product.DisallowPurchaseReason))
                                {
                                    <div class="gg-pd__why">
                                        <i class="ph-fill ph-info"></i>
                                        <span>@product.DisallowPurchaseReason</span>
                                    </div>
                                }
                            </div>
                        }

                        <div class="gg-pd__facts">
                            @if (isTime)
                            {
                                <div class="gg-pd__fact">
                                    <i class="ph ph-desktop"></i>
                                    <div>
                                        <small>@ShellText.Get(ShellText.PD_WHERE)</small>
                                        <ProductTimeHostGroups @key="ProductId" Product="@product" />
                                    </div>
                                </div>

                                @if (time.ExpirationOptions != ProductTimeExpirationOptionType.None)
                                {
                                    <div class="gg-pd__fact">
                                        <i class="ph ph-hourglass-low"></i>
                                        <div>
                                            <small>@ShellText.Get(ShellText.PRODUCT_EXPIRY)</small>
                                            @if (time.ExpirationOptions.HasFlag(ProductTimeExpirationOptionType.ExpireAtDayTime))
                                            {
                                                <b>@ShellText.Get(ShellText.PD_EXPIRES_AT_DAYTIME, ProductHelpers.GetDayTimeFromMinute(time.ExpireAtDayTimeMinute))</b>
                                            }
                                            @if (time.ExpirationOptions.HasFlag(ProductTimeExpirationOptionType.ExpireAfterTime))
                                            {
                                                <b>@ShellText.Get(ShellText.PD_EXPIRES_AFTER, ProductHelpers.GetExpiresAfterText(time, LocalizationService))</b>
                                            }
                                            @if (time.ExpirationOptions.HasFlag(ProductTimeExpirationOptionType.ExpiresAtLogout))
                                            {
                                                <b>@ShellText.Get(ShellText.PD_EXPIRES_AT_LOGOUT)</b>
                                            }
                                        </div>
                                    </div>
                                }
                            }

                            @if (product.PurchaseAvailability is not null)
                            {
                                <div class="gg-pd__fact">
                                    <i class="ph ph-shopping-cart-simple"></i>
                                    <div>
                                        <small>@ShellText.Get(ShellText.PRODUCT_BUY_WINDOW)</small>
                                        @foreach (var line in ProductHelpers.GetPurchaseAvailabilities(product, !_showMore, LocalizationService))
                                        {
                                            <b>@line</b>
                                        }
                                    </div>
                                </div>
                            }

                            @if (isTime && time.UsageAvailability is not null)
                            {
                                <div class="gg-pd__fact">
                                    <i class="ph ph-play-circle"></i>
                                    <div>
                                        <small>@ShellText.Get(ShellText.PRODUCT_USE_WINDOW)</small>
                                        @foreach (var line in ProductHelpers.GetUsageAvailabilities(product, !_showMore, LocalizationService))
                                        {
                                            <b>@line</b>
                                        }
                                    </div>
                                </div>
                            }

                            @if (HasMoreAvailability)
                            {
                                <button type="button" class="gg-pd__more" @onclick="ToggleMore">
                                    <span>@ShellText.Get(_showMore ? ShellText.PRODUCT_LESS : ShellText.PRODUCT_MORE)</span>
                                    <i class="ph-bold @(_showMore ? "ph-caret-up" : "ph-caret-down")"></i>
                                </button>
                            }
                        </div>
                    </div>
                </section>

                @if (!string.IsNullOrWhiteSpace(product.Description))
                {
                    <section class="gg-pd__about">
                        <div class="gg-cap gg-cap--rule"><span>@ShellText.Get(ShellText.PD_ABOUT)</span><span class="gg-cap__rule"></span></div>
                        <p class="gg-pd__desc">@product.Description</p>
                    </section>
                }

                @if (isBundle && product.BundledProducts.Any())
                {
                    <section class="gg-pd__bundle">
                        <div class="gg-cap gg-cap--rule"><span>@ShellText.Get(ShellText.PD_INCLUDES)</span><span class="gg-cap__rule"></span></div>
                        <div class="gg-pd__bundle-row">
                            @foreach (var bundled in product.BundledProducts)
                            {
                                @* Not keyed by id: a bundle can hold the same product twice. *@
                                <ProductDetailsBundledProduct @key="@bundled" ProductId="@bundled.Id" Quantity="bundled.Quantity" />
                            }
                        </div>
                    </section>
                }

                @if (CanPurchase && ViewState.RelatedProducts != null && ViewState.RelatedProducts.Any())
                {
                    <section class="gg-pd__related">
                        <div class="gg-cap gg-cap--rule"><span>@ShellText.Get(ShellText.SHOP_RELATED)</span><span class="gg-cap__rule"></span></div>
                        <div class="gg-pd__related-row">
                            @foreach (var related in ViewState.RelatedProducts)
                            {
                                <ProductCard @key="related.Id" Product="@related" />
                            }
                        </div>
                    </section>
                }
            }
        </div>

        @if (CanPurchase)
        {
            <aside class="gg-pd__cart">
                <GizOrder />
            </aside>
        }
    </div>
</div>
