@namespace Gizmo.Client.UI.Pages
@inherits CustomDOMComponentBase

<ConfirmationCodeCard EditContext="@PasswordRecoveryConfirmationViewService.EditContext"
                      Title="@LocalizationService.GetString(nameof(Gizmo.Client.UI.Resources.Properties.Resources.GIZ_PASSWORD_RECOVERY_FORGOT_PASSWORD))"
                      Subtitle="@ViewState.ConfirmationCodeMessage"
                      ConfirmationCode="@ViewState.ConfirmationCode"
                      CodeChanged="@((string value) => PasswordRecoveryConfirmationViewService.SetConfirmationCode(value))"
                      CodeExpression="@(() => ViewState.ConfirmationCode)"
                      IsLoading="@ViewState.IsLoading"
                      IsValid="@ViewState.IsValid"
                      HasError="@ViewState.HasError"
                      ErrorMessage="@ViewState.ErrorMessage"
                      OnConfirm="@PasswordRecoveryConfirmationViewService.Confirm"
                      OnCloseError="@OnCloseButtonClickHandler"
                      SecondsLeft="@ViewState.SecondsLeft"
                      TimerExpired="@ViewState.TimerExpired"
                      OnResend="@ResendCode"
                      SignUpSubtitle="false">
    <HeaderLinks>
        <div class="giz-back-button" @onclick="@(() => NavigationService.NavigateTo(ClientRoutes.PasswordRecoveryRoute))">
            <Icon SVGIcon="Icons.ArrowLeft_Client" Size="IconSizes.ExtraSmall" />
        </div>
        @if (UserRegisterConfigurationViewState.IsEnabled)
        {
            <div class="giz-login-new-user">@LocalizationService.GetString(nameof(Gizmo.Client.UI.Resources.Properties.Resources.GIZ_LOGIN_NOT_A_MEMBER)) <a @onclick="UserLoginService.OpenRegistrationAsync">@LocalizationService.GetString(nameof(Gizmo.Client.UI.Resources.Properties.Resources.GIZ_LOGIN_SIGN_UP_NOW))</a></div>
        }
    </HeaderLinks>
</ConfirmationCodeCard>
