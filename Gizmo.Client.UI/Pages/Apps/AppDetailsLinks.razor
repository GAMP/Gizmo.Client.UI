@namespace Gizmo.Client.UI.Pages
@inherits CustomDOMComponentBase

<ClientTab ControlsPosition="TabControlsPositions.Internal">
    @foreach (var link in @AppLinkViewStateLookupService.GetStatesAsync().GetAwaiter().GetResult().Where(a => a.ApplicationId == ApplicationId))
    {
        <AppDetailsLink Link="@link" @key="link.AppLinkId" />
    }
</ClientTab>
